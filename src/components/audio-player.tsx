// src/components/audio-player.tsx  (stub, replaced in Task 10)
export function MuteButton({ label }: { label: string }) {
  return <span className="sr-only">{label}</span>;
}
export function AudioPlayer() {
  return null;
}
